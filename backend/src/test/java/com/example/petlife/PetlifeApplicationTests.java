package com.example.petlife;

import org.junit.jupiter.api.Test;

// Full Spring context startup requires a live PostgreSQL instance.
// Run `docker compose up --build` and execute `./mvnw verify -Pit` for end-to-end tests.
// Unit tests for individual components live in their respective *Test classes.
class PetlifeApplicationTests {

    @Test
    void contextLoads() {
        // no-op: verified by unit tests in this module
    }
}
